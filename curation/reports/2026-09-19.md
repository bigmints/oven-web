# Oven curation — 2026-09-19

Daily consumer-only curation run. Research-only: no candidate dependencies or
applications were installed or executed. Linux source checks do not establish
macOS Oven runtime compatibility.

## Inherited entries (source-reviewed)

### Excalidraw — source-reviewed (retained)
- Commit `97c68dd371e13c017a8dcca49f8b3995ba7890a8`, pushed 2026-09-19.
- License: MIT. Root package `excalidraw-monorepo` exposes a `start` script
  (`yarn --cwd ./excalidraw-app start`). Vite-based web app; no native toolchain or external
  service required to launch.
- Passes the supported setup path (public GitHub source, Node workspace,
  runnable `start` script). Promoted `unverified` → `source-reviewed`.
- Runtime launch not tested on macOS Oven.

### Actual Budget — source-reviewed (retained, with caveats)
- Commit `749fa264571ce53940f055192ee2e0cc26b3e172`, pushed 2026-09-19.
- License: MIT. Root package `actual` exposes a `start` script
  (`yarn start:browser`).
- Passes the Node workspace + start-script gate, but has native dependencies
  (better-sqlite3, bcrypt, argon2, sharp) and an optional sync server that may
  need native toolchains Oven does not provision. Retained as `source-reviewed`
  with these requirements disclosed; not promoted to `verified`.

## New candidate considered

### DoHabit — accepted (source-reviewed)
- Repository: https://github.com/iNikAnn/DoHabit
- Commit `be269c7b64ea1f7b99ad3e684ee3daa5b4b46bb0`, pushed 2026-09-17.
- License: AGPL-3.0. Root package `dohabit` exposes a `dev` script (`vite`).
- Vite + React app, local-first (IndexedDB), no native dependencies or external
  services to launch. Fills the Wellbeing category (currently empty).
- Scores (0–2): usefulness 2, local value 2, setup fit 2, first-use clarity 2,
  maintenance 1 (64 stars, active). Total 9/10, no setup-fit zero.
- Runtime launch not tested on macOS Oven.

## Rejected candidates (with reasons)

- **Simple Photo Gallery** (SimplePhotoGallery/core): CLI tool, not a launchable
  app; no `dev`/`start`/`serve` script. Fails the launch-target gate.
- **Mise** (lopatnov/mise): recipe manager requiring MongoDB + Docker Compose.
  Oven cannot provision a database or Docker. Fails the setup gate.
- **Nextcloud** (nextcloud/server): PHP server, not a Node workspace. Fails the
  supported setup path.
- **FileToLink / mshare** (file sharing): thin or abandoned; not a maintained
  consumer app.
- **LocalSend**: native Rust app, not a Node workspace.
- **world-famous-book** (RS-Arafath): no license file; 3 stars; last push
  2026-05-12. Fails the license and maintenance-confidence gates.
- **talliofi** (Emmanuelnoi): local-first finance, but 1 star, 2 contributors,
  last push 2026-07-17; overlaps Actual Budget. Maintenance confidence too low.
- **Recipe managers (RecipeBox, etc.)**: require MongoDB/PostgreSQL/Docker.
  Fails the setup gate.

## Checks run
- `node scripts/validate-catalog.mjs`
- `node scripts/build-site.mjs`
- `node --test scripts/site.test.mjs`

## Limitations
- All three entries are `source-reviewed`, not `verified`: no macOS Oven runtime
  launch, HTTP endpoint, or primary-task test was performed (this is a
  research-only job; no code was installed or executed).
- Actual Budget's native modules and sync server are unproven on Oven.
- Deployment (GitHub Pages) is triggered by the push but was not separately
  verified in this run.

## Independent source correction

The first run oversimplified two start commands and overstated native-dependency conclusions. A subsequent direct read of the pinned package manifests corrected Excalidraw to `yarn --cwd ./excalidraw-app start` (Yarn 1.22.22) and Actual Budget to `yarn start:browser` (Yarn 4.17.1, Node >=22.18.0). Transitive native-build and external-service claims remain unverified. The catalog now records exact structured launch recipes, and the publisher verifies them against upstream before future pushes. This corrects source claims only; no application was installed.
